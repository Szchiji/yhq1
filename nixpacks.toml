# Railway Nixpacks 配置
# 删除 nixPkgs，让 Nixpacks 完全自动配置 Python 环境

providers = ["python"]

[variables]
PIP_NO_CACHE_DIR = "1"

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "cd backend && uvicorn app.main:app --host 0.0.0.0 --port ${PORT:-8000}"